import styled from "styled-components"

export const OrderConfirmBox = styled.div`
        width: 100%;
        background-color: #fff;
        border-radius: 4px;
        margin-top: 24px;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 32px;
        padding: 64px 0;
    `

export const OrderConfirmText = styled.span`
        font-size: 20px;
        font-weight: 700;
    `

export const OrderConfirmVector = styled.img`
        height: 265px;
    `

export const GoBackBtn = styled.a`
        background-color: #009EDD;
        text-transform: uppercase;
        color: #fff;
        font-size: 14px;
        font-weight: 700;
        padding: 11px 60px;
        border-radius: 4px;
        border: none;
        cursor: pointer;
        text-decoration: none;
    `